import { Typography } from "@mui/material";


export default function CheckoutPage(){   //create component
    return(
        <Typography variant='h3'>
            Only logged in users should see this!
        </Typography>
    )
}